{
  "name": "aws-lambda-fixie-example",
  "version": "1.0.0",
  "description": "Example AWS Lambda function using Fixie for static IP addresses",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "lambda",
    "fixie",
    "static-ip",
    "proxy"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2"
  }
}
